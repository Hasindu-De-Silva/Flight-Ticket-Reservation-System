spring :
  datasource :
    url : jdbc:mysql://localhost:3306/airline1?createDatabaseIfNotExist=true&serverTimezone=UTC
    username : root
    password : Hasiya@030729
    driver-class-name : com.mysql.cj.jdbc.Driver
  jpa :
    hibernate :
      ddl-auto : update
    show-sql : true
    database-platform : org.hibernate.dialect.MySQL8Dialect

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML